# X-Archiver リリースノート

## v1.0.0 (2025-12-30) - 初版リリース

### 概要
Xのスレッドを完全なアーカイブとしてMarkdownに書き出すための基本機能が完成。

### 新機能
- **スレッド自動追跡**: 最初のツイートURLから連なる連続投稿を自動取得。
- **OP（投稿主）フィルタリング**: リプライ欄の他人の投稿を除外し、メインコンテンツのみを抽出。
- **メディアのローカル保存**: `gallery-dl` を利用し、動画と画像を最高画質で自動取得。
- **リッチなMarkdown出力**: 動画は `<video>` タグ、画像は `![Image]` としてローカルパスで埋め込み。
- **認証バイパス**: ブラウザからエクスポートした `cookies.json` を使用して、ログイン必須のスレッドも取得可能。
- **自動形式変換**: 拡張機能（Get cookies.txt locally等）から出力された生のクッキーリストをPlaywright形式に自動変換して。

### 技術スタック
- **Python 3.14**
- **Playwright**: ブラウザ自動操作とDOM抽出。
- **gallery-dl**: 高性能なメディアダウンローダー。

### 今後の課題
- 引用ツイートの詳細展開。
- スレッドが非常に長い場合の分割取得最適化。
- 出力フォーマット（HTMLやPDF）の拡充。
